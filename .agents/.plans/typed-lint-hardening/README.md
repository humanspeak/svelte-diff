# Typed Lint Hardening Plans

Generated by the improve skill on 2026-07-17. This batch adapts the lint/test
hardening intent from `svelte-markdown` to this pnpm workspace. Read the plan
fully before starting, honor its STOP conditions, and update the status below
when the execution blocker is cleared.

## Execution order & status

| Plan | Title                                   | Priority | Effort | Depends on | Status                                                                              |
| ---- | --------------------------------------- | -------- | ------ | ---------- | ----------------------------------------------------------------------------------- |
| 001  | Lint tests and type-check promise usage | P2       | M      | —          | DONE — approved at `ebdd8ee`; scoped lint, 49 unit tests, and 75 E2E tests verified |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- Root `pnpm run check` and `pnpm run test:only` are green as of 2026-07-17.
  Docs has four existing type errors plus ten warnings, and its unit command
  fails because Vitest 4 removed `test.workspace`. These are a comparison
  baseline, not a dependency: 001 may proceed but must introduce no new docs
  diagnostics and must not repair the unrelated failures.
- The dependency/toolchain upgrade is committed. Remove only the obsolete
  `**/*.svelte.ts` ESLint exclusion after proving the upgraded parser works.

## Execution record

- Approved on 2026-07-17 at commit `ebdd8ee` on branch
  `advisor/001-typed-lint-hardening` in worktree
  `/private/tmp/svelte-diff-typed-lint-exec`.
- Independent review verified targeted Trunk lint, all eight typed-lint guards,
  root type checking, 49/49 unit tests, and 75/75 Playwright tests.
- Docs reproduced its accepted four-error/ten-warning check baseline and Vitest
  `test.workspace` startup failure. Full Trunk reproduced the accepted three
  lint issues, three formatting issues, and hermetic tool failures; none were
  introduced by this plan.

## Findings considered and rejected

- Enabling all of `typescript-eslint`'s `recommendedTypeChecked` preset: rejected
  for this batch. A read-only dry run produced 13 errors in the root package and
  43 errors in the owned portion of `docs/`, mostly unrelated unsafe-type rules;
  a full docs-tree run was also impractically heavy. This plan enables the two
  promise-correctness rules directly and leaves broader typed-lint adoption for
  a separately scoped cleanup.
- Type-aware linting for `docs/src/lib/shadcn/**`: rejected because this is a
  large imported/generated-style component tree with an existing relaxed ESLint
  override. It remains under ordinary ESLint, but is excluded from the
  type-aware promise-rule block.
- Fixing `docs/scripts/generate-social-images.ts:62` in this batch: deferred.
  The current working tree removes its package script and Puppeteer dependency,
  so the file's future is being decided by concurrent work. If retained, handle
  its floating `main()` promise and missing dependency in a separate plan.
